# hpa

Reserved for future Horizontal Pod Autoscaler manifests targeting the `api` and `worker` Deployments (`k8s/api/`, `k8s/worker/`). No HPA resources are defined yet — scaling today is manual via `kubectl scale`. See [Kubernetes Deployment → Future extensions](../../docs/platform/kubernetes-deployment.md#future-extensions) for details.
