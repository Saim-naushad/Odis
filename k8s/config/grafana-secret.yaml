# Development template only. Replace values before production deployment.
# Prefer: kubectl create secret generic odis-grafana --namespace odis \
#   --from-literal=GF_SECURITY_ADMIN_USER='<admin-user>' \
#   --from-literal=GF_SECURITY_ADMIN_PASSWORD='<generated-password>'
apiVersion: v1
kind: Secret
metadata:
  name: odis-grafana
  namespace: odis
  labels:
    app.kubernetes.io/part-of: odis
    app.kubernetes.io/component: grafana
type: Opaque
stringData:
  GF_SECURITY_ADMIN_USER: admin
  GF_SECURITY_ADMIN_PASSWORD: CHANGE_ME
