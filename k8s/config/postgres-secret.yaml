# Development template only. Replace values before production deployment.
# Prefer: kubectl create secret generic odis-postgres --namespace odis \
#   --from-literal=POSTGRES_DB=odis \
#   --from-literal=POSTGRES_USER=odis \
#   --from-literal=POSTGRES_PASSWORD='<generated-password>' \
#   --from-literal=DATABASE_URL='postgresql+psycopg://odis:<generated-password>@postgres:5432/odis'
apiVersion: v1
kind: Secret
metadata:
  name: odis-postgres
  namespace: odis
  labels:
    app.kubernetes.io/part-of: odis
    app.kubernetes.io/component: postgres
type: Opaque
stringData:
  POSTGRES_DB: odis
  POSTGRES_USER: odis
  POSTGRES_PASSWORD: CHANGE_ME
  DATABASE_URL: postgresql+psycopg://odis:CHANGE_ME@postgres:5432/odis
